import React from "react";

const styles = {
  banner: {
    width: "100%"
  }
};

const Header: React.FC = () => {
  return <img style={styles.banner} src="https://i.imgur.com/xxxV8Jo.png" />;
};

export default Header;
